# Copy to .env and fill in. Loaded automatically by firebase-tools at deploy.
# The Web API key is a client identifier, not a secret, but keep .env out of git.
CONTROL_HUB_WEB_API_KEY=your-web-api-key
CONTROL_HUB_DATABASE_URL=https://your-project-default-rtdb.firebaseio.com
